# 快速推送代码到 GitHub

## 当前状态

- ✅ 当前分支：`master`
- 📝 待提交文件：
  - `.github/workflows/main.yml` (GitHub Actions 配置)
  - `docs/` 目录（包含多个文档）
  - `README.md` (已修改)

## 操作步骤

### 步骤 1: 提交当前更改

在项目根目录执行以下命令：

```bash
# 1. 添加所有更改
git add .

# 2. 提交更改（使用规范的提交信息）
git commit -m "feat: 添加文档和 GitHub Actions 配置"
```

### 步骤 2: 创建 GitHub 仓库（如果还没有）

1. 访问 [GitHub](https://github.com)
2. 点击右上角的 "+" 按钮，选择 "New repository"
3. 填写仓库信息：
   - **Repository name**: `my-vue-app` (或你喜欢的名称)
   - **Description**: `Vue 3 + Vite 现代化前端项目`
   - **Visibility**: 选择 Public 或 Private
   - **不要**勾选 "Initialize this repository with a README"（因为本地已有代码）
4. 点击 "Create repository"
5. 复制仓库地址（例如：`https://github.com/your-username/my-vue-app.git`）

### 步骤 3: 添加远程仓库

将 `YOUR_USERNAME` 和 `YOUR_REPO_NAME` 替换为你的实际信息：

```bash
# 使用 HTTPS（推荐新手）
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 或者使用 SSH（需要配置 SSH 密钥）
git remote add origin git@github.com:YOUR_USERNAME/YOUR_REPO_NAME.git

# 验证远程仓库配置
git remote -v
```

### 步骤 4: 推送代码到 GitHub

```bash
# 首次推送，设置上游分支
git push -u origin master
```

**注意**：如果 GitHub 仓库的默认分支是 `main` 而不是 `master`，使用：

```bash
# 方法 1: 重命名本地分支为 main
git branch -M main
git push -u origin main

# 方法 2: 直接推送到 main 分支
git push -u origin master:main
```

## 完整命令序列（一键执行）

**Windows (PowerShell)**:

```powershell
git add .
git commit -m "feat: 添加文档和 GitHub Actions 配置"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git push -u origin master
```

**Linux/macOS (Bash)**:

```bash
git add .
git commit -m "feat: 添加文档和 GitHub Actions 配置"
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git push -u origin master
```

## 常见问题处理

### 问题 1: 远程仓库已存在内容

如果远程仓库已经初始化了 README 或其他文件，需要先拉取：

```bash
# 拉取远程内容并允许不相关的历史记录合并
git pull origin master --allow-unrelated-histories

# 解决可能的冲突后，再推送
git push -u origin master
```

### 问题 2: 认证失败

如果遇到认证问题，可以：

1. **使用个人访问令牌（Personal Access Token）**

   - 访问 GitHub Settings > Developer settings > Personal access tokens > Tokens (classic)
   - 点击 "Generate new token"
   - 选择权限（至少需要 `repo` 权限）
   - 复制生成的 token
   - 推送时使用 token 作为密码

2. **使用 SSH 密钥（推荐）**

   ```bash
   # 生成 SSH 密钥
   ssh-keygen -t ed25519 -C "your_email@example.com"

   # 复制公钥内容
   cat ~/.ssh/id_ed25519.pub

   # 在 GitHub 中添加 SSH 密钥
   # Settings > SSH and GPG keys > New SSH key
   ```

3. **使用 GitHub CLI**

   ```bash
   # 安装 GitHub CLI
   # Windows: choco install gh
   # macOS: brew install gh
   # Linux: 查看 https://cli.github.com/

   # 登录
   gh auth login
   ```

### 问题 3: 推送被拒绝

如果推送被拒绝，可能是因为：

1. **远程仓库有新的提交**

   ```bash
   # 先拉取远程更改
   git pull origin master --rebase

   # 然后推送
   git push -u origin master
   ```

2. **分支保护规则**
   - 检查 GitHub 仓库的分支保护规则
   - 可能需要通过 Pull Request 推送

### 问题 4: 文件名编码问题

如果遇到中文文件名显示乱码：

```bash
# 设置 Git 使用 UTF-8 编码
git config --global core.quotepath false
git config --global gui.encoding utf-8
git config --global i18n.commitencoding utf-8
git config --global i18n.logoutputencoding utf-8
```

## 验证推送结果

推送成功后：

1. 访问你的 GitHub 仓库：`https://github.com/YOUR_USERNAME/YOUR_REPO_NAME`
2. 检查文件是否都正确上传
3. 检查提交历史是否正确显示
4. 检查 GitHub Actions 是否自动运行（如果有配置）

## 后续操作

### 设置分支保护规则

1. 进入仓库 Settings > Branches
2. 点击 "Add rule"
3. 输入分支名称（如 `master` 或 `main`）
4. 启用必要的保护选项：
   - Require pull request reviews before merging
   - Require status checks to pass before merging
   - Require conversation resolution before merging
5. 保存设置

### 配置 GitHub Actions

如果已经配置了 `.github/workflows/main.yml`，推送后 GitHub Actions 会自动运行。

### 设置 GitHub Pages（如果需要）

1. 进入仓库 Settings > Pages
2. 选择源分支（如 `main` 或 `master`）
3. 选择目录（如 `/docs` 或 `/`）
4. 保存设置

## 需要帮助？

如果你遇到任何问题，可以：

1. 查看 Git 状态：`git status`
2. 查看远程仓库配置：`git remote -v`
3. 查看提交历史：`git log --oneline`
4. 查看详细的错误信息
